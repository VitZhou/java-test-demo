<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.demo.server.resource.mapper.UserMapper">
    <select id="findByName" resultType="com.test.demo.server.resource.entity.User">
        SELECT * FROM test_user WHERE name = #{name}
    </select>
</mapper>